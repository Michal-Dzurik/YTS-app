<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/yts_green" /> <!-- Green color -->
            <corners android:radius="8dp" /> <!-- Rounded corners, adjust the radius as needed -->
        </shape>
    </item>
    <item>
        <ripple android:color="#2C6016"> <!-- Ripple effect color, adjust as needed -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/yts_green" /> <!-- Default button color -->
                    <corners android:radius="8dp" /> <!-- Rounded corners, adjust the radius as needed -->
                </shape>
            </item>
        </ripple>
    </item>
</selector>