<?xml version="1.0" encoding="utf-8"?>
<LinearLayout android:id="@+id/button"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="left"
    android:paddingHorizontal="7dp"
    android:paddingVertical="5dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:clickable="true"
        android:focusable="true"
        android:layout_weight="1"
        app:cardCornerRadius="6dp"
        android:foreground="@drawable/movie_ripple"
        android:background="@drawable/movie_ripple">

        <ImageView
            android:id="@+id/movieImage"
            android:src="@drawable/movie_template"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"

            />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/movieTitle"
        android:text="Love, Rosie"
        android:textSize="14sp"
        android:layout_marginTop="7dp"
        android:textStyle="bold"
        android:textColor="@color/heading_white"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/transparent"
        />

    <TextView
        android:id="@+id/movieYear"
        android:text="2013"
        android:textSize="13sp"
        android:textColor="@color/text_grey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/transparent"
        android:layout_marginBottom="5dp"
        />

</LinearLayout>